namespace pingine.Game.Graphics
{
    /*
    public class AnimatedSprite : Sprite
    {
        // spritebox ID, pondered by its duration
        // duration in milliseconds or in frames? we don't want it to depend on FPS, but do we want it to depend on UPS?
        public Pondered<SpriteBoxId, ulong> Frames { get; private set; }
        public bool Loops { get; private set; }

        public AnimatedSprite(SpriteSet spriteSet, Pondered<SpriteBoxId, ulong> frames, bool loops)
            : base(spriteSet)
        {
            Frames = frames;
            Loops = loops;
        }
    }
    */
}
